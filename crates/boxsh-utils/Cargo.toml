[package]
name = "boxsh-utils"
description = "Command support for boxsh."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
boxsh-core = { path = "../boxsh-core" }

[features]
default = []
