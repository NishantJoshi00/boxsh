{
  "name": "nobox",
  "version": "0.0.1",
  "description": "A fake sandbox for AI agents: persistent virtual filesystem + bash, in WASM.",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node test/smoke.mjs && node test/api.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
